export type Role = "administrator" | "manager" | "viewer" | "employee";
export type Permission = "employees.manage" | "projects.create" | "projects.edit" | "projects.delete" | "projects.assign" | "tasks.manage" | "tasks.progress";

